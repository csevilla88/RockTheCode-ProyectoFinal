[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: cualquier ruta no encontrada → index.html (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
